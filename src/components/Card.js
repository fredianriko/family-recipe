import React from "react";
import CardItems from "./CardItems";

function Card(props) {
  return (
    <div className="card-group">
      <CardItems />
    </div>
  );
}

export default Card;
